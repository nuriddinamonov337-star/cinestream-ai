"""CineStream AI — Python Telegram bot (aiogram 3 + FastAPI webhook).

Port of the TypeScript bot in src/lib/telegram/. Same Supabase schema,
same FSM states, same admin flows — rewritten in Python for Railway deploy.
"""
